const input = require("fs").readFileSync("../dev/11654/stdin").toString();

/**
 * @param {string} input
 * @return {void}
 */
function solution(input) {
  console.log(input.charCodeAt());
}

solution(input);
